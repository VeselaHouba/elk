filebeat.prospectors:
  # Application logs
- input_type: log
  paths:
    - /tmp/log/hcpay.log
  document_type: "hcp_app"
  ignore_older: 2h
  multiline.pattern: '^\[[0-9]{4}-[0-9]{2}-[0-9]{2}'
  multiline.negate: true
  multiline.match: after
  multiline.max_lines: 1000