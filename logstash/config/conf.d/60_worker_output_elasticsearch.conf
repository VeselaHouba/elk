output {
		elasticsearch {
			document_id => "%{fingerprint}"
			hosts => [ "localhost" ]
			template => "/opt/elk/logstash/elasticsearch-templates/elasticsearch-template_hci_33.json"
			index => "logstash-h-%{+YYYY.MM.dd}"
			template_overwrite => true
		}
}
